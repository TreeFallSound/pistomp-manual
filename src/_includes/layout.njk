<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} — pi-Stomp Manual</title>
  <link rel="icon" href="{{ '/assets/images/favicon.ico' | url }}" sizes="any">
  <link rel="stylesheet" href="{{ '/assets/css/style.css' | url }}">
</head>
<body>
  <div class="layout">
    <nav class="sidebar">
      <div class="sidebar-header">
        <a href="{{ '/' | url }}" class="sidebar-title">pi-Stomp Manual</a>
      </div>
      <ul class="nav-list">
      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-section">
          <a href="{{ entry.url | url }}" class="nav-section-title{% if page.url == entry.url %} active{% endif %}">{{ entry.title }}</a>
          {%- if entry.children.length %}
          <ul class="nav-pages">
            {%- for child in entry.children %}
            <li><a href="{{ child.url | url }}" class="nav-link{% if page.url == child.url %} active{% endif %}">{{ child.title }}</a></li>
            {%- endfor %}
          </ul>
          {%- endif %}
        </li>
      {%- endfor %}
      </ul>
    </nav>
    <main class="content">
      {{ content | safe }}
    </main>
  </div>
</body>
</html>
